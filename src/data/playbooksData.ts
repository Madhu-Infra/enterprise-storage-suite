import { EmergencyPlaybook } from '../types/storage';

export const PLAYBOOKS_DATA: EmergencyPlaybook[] = [
  {
    id: 'p1-san-slow-drain',
    title: 'SAN Fabric ISL Congestion & Slow-Drain Device Triage',
    severity: 'P1 - Critical Outage',
    platforms: ['cisco', 'brocade'],
    estimatedTtr: '15-30 mins',
    summary: 'A slow-drain device (such as a bogged-down host HBA, locked tape drive, or misbehaving ASIC) fails to return buffer-to-buffer credits, backing up frames across ISLs and causing latency spikes across unrelated workloads.',
    symptoms: [
      'Disproportionate latency spikes across completely independent storage arrays sharing ISLs',
      'ESXi host disconnects or multipath path state thrashing ("Path Dead" / "Path Active")',
      'Brocade: "tim_txcnd" or "tx_wait" counters incrementing rapidly in porterrshow',
      'Cisco: "credit-loss" or "Zero Tx" counters rapidly climbing on E-Ports and F-Ports',
    ],
    rootCauses: [
      'Host HBA firmware or OS driver lockup unable to process frame payloads',
      'Dirty fiber optic connector or degraded SFP transceiver suffering marginal loss of light',
      'Speed mismatch between fast 32G target array and 8G/16G legacy host edge port',
      'Oversubscribed ISL trunks with asymmetrical flow control',
    ],
    triageSteps: [
      {
        stepNumber: 1,
        instruction: 'Inspect ISLs and Edge Ports for active credit exhaustion and TxWait stalls',
        command: 'Brocade: porterrshow | grep -E "tx_wait|tim_txcnd"\nCisco: show interface counters detailed | include "Credit Loss|Zero Tx|TxWait"',
        expectedOutput: 'Zero or static numbers. In healthy fabrics, TxWait should be 0 or under 100/day.',
        abnormalIndication: 'Thousands of TxWait increments per minute on an edge F-Port (this is the root offender!) or ISL E-Port.',
        actionIfAbnormal: 'Identify the exact edge F-Port where the issue begins (do NOT disable ISLs, find the edge host port!).',
      },
      {
        stepNumber: 2,
        instruction: 'Trace the offending port to host identity via FLOGI / Name Server',
        command: 'Brocade: nsshow | grep -B 2 <port_id>\nCisco: show flogi database interface fc<port_num>',
        expectedOutput: 'Clean host WWPN, OS hostname, and device-alias mapping.',
        abnormalIndication: 'Offending port maps to a specific ESXi or physical database server HBA.',
        actionIfAbnormal: 'Notify server administrator immediately that this specific HBA is starving the SAN.',
      },
      {
        stepNumber: 3,
        instruction: 'Emergency Isolation - Port Disable to restore fabric health for the rest of datacenter',
        command: 'Brocade: portdisable <port_index>\nCisco: configure terminal ; interface fc<port> ; shutdown ; exit',
        expectedOutput: 'Port transitions to disabled/down state.',
        abnormalIndication: 'ISL queue clears immediately within 5 seconds; storage latency drops from 50ms+ back to <1ms.',
        actionIfAbnormal: 'Confirm host multipathing fails over cleanly to redundant Fabric B.',
      },
    ],
    emergencyRemediation: [
      'Isolate the single offending edge port immediately using portdisable/shutdown (do not reboot switches or disable ISLs).',
      'Inspect physical optic Rx light level on the isolated port: Brocade `sfpshow <port>` or Cisco `show int fc<port> transceiver details`. Replace SFP if Rx power < -12 dBm.',
      'Check if host OS reboot cleans up the HBA driver queue; re-seat or replace fiber patch cable.',
      'Enable fabric-level Bottleneck Detection / FPIN (Fabric Performance Impact Notification) in firmware.',
    ],
    verificationChecklist: [
      'ISL queues clear: `statsclear ; sleep 30 ; porterrshow` shows 0 active increments',
      'Storage array latency telemetry returns to baseline (<1ms NVMe/All-Flash)',
      'Fabric B remains nominal and is not experiencing cascade effect',
    ],
  },
  {
    id: 'p1-netapp-aggregate-full',
    title: 'NetApp ONTAP Volume / Aggregate 100% Space Exhaustion',
    severity: 'P1 - Critical Outage',
    platforms: ['netapp'],
    estimatedTtr: '10-20 mins',
    summary: 'A volume or underlying aggregate hits 100% full due to runaway snapshots, unexpected database log dumps, or unreserved thin-provisioning, causing LUNs or NFS exports to enter read-only mode or offline.',
    symptoms: [
      'SAN LUNs freeze or drop off hosts with SCSI command write aborts',
      'NFS/CIFS clients receive "No space left on device" errors',
      'EMS event logs report `wafl.vol.full` or `wafl.aggr.full` at severity Alert/Emergency',
      'VMware ESXi VMs freeze in stunned state awaiting disk writes',
    ],
    rootCauses: [
      'Accumulated SnapMirror or local snapshot copies preventing block reclamation',
      'Auto-grow and space guarantee disabled on active write-heavy database volumes',
      'Thin provisioned LUNs overallocated on aggregate with <5% physical headroom',
    ],
    triageSteps: [
      {
        stepNumber: 1,
        instruction: 'Assess exact volume and aggregate space usage and snapshot spill',
        command: 'volume show -vserver <svm> -volume <vol> -fields size,available,percent-used,snapshot-percent-reserved,used\ndf -A <aggr_name>',
        expectedOutput: 'Available space > 10%, percent-used < 90%.',
        abnormalIndication: 'percent-used is 100%, available is 0KB.',
        actionIfAbnormal: 'Execute immediate snapshot triage to reclaim blocks instantly.',
      },
      {
        stepNumber: 2,
        instruction: 'Identify oldest or largest Snapshot copies for safe deletion',
        command: 'volume snapshot show -vserver <svm> -volume <vol> -fields snapshot,cumulative-total-percent,size',
        expectedOutput: 'List of snapshots with oldest backups.',
        abnormalIndication: 'Older snapshots locked by broken SnapMirror or Veeam/Commvault holding several hundred GBs.',
        actionIfAbnormal: 'Target oldest non-essential snapshot for immediate deletion.',
      },
      {
        stepNumber: 3,
        instruction: 'Reclaim capacity by deleting oldest stale snapshots',
        command: 'volume snapshot delete -vserver <svm> -volume <vol> -snapshot <snapshot_name>',
        expectedOutput: 'Snapshot deleted successfully; WAFL background scanner reclaims blocks in seconds.',
        abnormalIndication: 'Snapshot is locked by active clone or split. If so, modify volume size temporarily.',
        actionIfAbnormal: 'If snapshot cannot be deleted, expand volume: `volume size -vserver <svm> -volume <vol> -new-size +200GB`.',
      },
    ],
    emergencyRemediation: [
      'Delete oldest unneeded snapshots to free physical blocks instantly.',
      'Enable volume autogrow immediately: `volume modify -vserver <svm> -volume <vol> -space-mgmt-try-first volume_grow -autosize-mode grow_shrink -max-autosize <size>`.',
      'If aggregate is full: trigger volume move to an aggregate with free capacity: `volume move start -vserver <svm> -volume <vol> -destination-aggregate <other_aggr>`.',
      'Bring offline LUNs back online: `lun modify -vserver <svm> -path /vol/<vol>/<lun> -state online`.',
    ],
    verificationChecklist: [
      '`volume show -vserver <svm> -volume <vol>` shows state online and available space > 15%',
      '`lun show -vserver <svm>` shows all LUNs state: online',
      'Host filesystem unfreezes; database write transactions resume without SCSI reservation conflict',
    ],
  },
  {
    id: 'p1-powermax-srdf-split',
    title: 'PowerMax SRDF Replication Suspension & Emergency DR Failover',
    severity: 'P1 - Critical Outage',
    platforms: ['powermax'],
    estimatedTtr: '20-40 mins',
    summary: 'Dark fiber WAN link between primary and secondary datacenters drops, or primary array suffers power failure, causing SRDF Consistency Group to suspend and requiring emergency recovery.',
    symptoms: [
      'SRDF state changes from `Synchronized` or `Consistent` to `Suspended` or `Partitioned`',
      'Production hosts at DR site unable to access R2 devices because write-disabled flag is active',
      'Unisphere alerts: "RDF Group link failure detected"',
    ],
    rootCauses: [
      'Inter-datacenter DWDM link fiber cut or GigE/FC IP link failure',
      'Primary datacenter emergency power outage (generator failover glitch)',
      'RDF Director (RA) port hardware failure or optic pull',
    ],
    triageSteps: [
      {
        stepNumber: 1,
        instruction: 'Query SRDF group state and invalid track counts from surviving target array',
        command: 'symrdf -sid <target_sid> -rdfg <rdfg> -cg <cg_name> query',
        expectedOutput: 'Pair State: Synchronized / Consistent.',
        abnormalIndication: 'Pair State: Suspended, Partitioned, or Transmit-Idle with millions of invalid tracks.',
        actionIfAbnormal: 'Assess whether WAN link is recoverable or if full disaster failover is mandated by leadership.',
      },
      {
        stepNumber: 2,
        instruction: 'If Site A is confirmed down: Execute emergency SRDF Failover to enable R2 Read/Write',
        command: 'symrdf -sid <target_sid> -rdfg <rdfg> -cg <cg_name> failover -force',
        expectedOutput: 'Command completes; R2 devices transition to Write-Enabled (RW).',
        abnormalIndication: 'Consistency group errors out due to in-flight I/O. Use `-force` or split individual devices if required.',
        actionIfAbnormal: 'Verify R2 devices are now presented to secondary cluster hosts.',
      },
      {
        stepNumber: 3,
        instruction: 'Rescan storage adapters on DR ESXi or bare-metal host clusters',
        command: 'esxcli storage core adapter rescan --all\nsymaccess -sid <target_sid> show view <dr_masking_view>',
        expectedOutput: 'Host cluster discovers identical LUN UUIDs; datastores mount cleanly without re-signature.',
        abnormalIndication: 'Volumes appear snapshot-mounted or inactive.',
        actionIfAbnormal: 'Force mount VMFS volume keeping original signature.',
      },
    ],
    emergencyRemediation: [
      'Execute `symrdf failover` with consistency group integrity preserved.',
      'Mount storage on secondary cluster and start high-priority application tiers first (DB -> App -> Web).',
      'Once WAN link is restored, NEVER run failback blindly without determining delta direction! Run `symrdf query` first.',
      'To resynchronize once primary is up: `symrdf failback` or `symrdf establish` after verifying primary state.',
    ],
    verificationChecklist: [
      'Target array `symrdf verify -failover` returns true',
      'Secondary hosts show all paths active in ALUA multipathing',
      'Change window logged for post-recovery resync',
    ],
  },
  {
    id: 'p1-fabric-segmentation',
    title: 'Brocade / Cisco SAN Fabric Segmentation & Domain ID Conflict',
    severity: 'P1 - Critical Outage',
    platforms: ['cisco', 'brocade'],
    estimatedTtr: '20-30 mins',
    summary: 'An ISL between two SAN switches drops into an isolated or segmented state due to mismatched zoning policies, duplicate Domain IDs, or incompatible switch parameters, splitting the fabric in half.',
    symptoms: [
      'ISL E-Port goes into "Segmented" or "Disabled" state instead of online/trunking',
      'Hosts connected to Switch A cannot discover LUN targets attached to Switch B',
      'Brocade switchshow displays: `Port X: E-Port Segmented (Zone Conflict / Incompatible Parameters)`',
      'Cisco MDS logs show `VSAN x: ISL down - Domain ID overlap`',
    ],
    rootCauses: [
      'Duplicate Domain ID assigned to both switches in the same VSAN/Fabric',
      'Zone database conflict: different default zoning policies (permit vs deny) or checksum mismatch',
      'Incompatible BB_Credit or trunking parameters across switches',
    ],
    triageSteps: [
      {
        stepNumber: 1,
        instruction: 'Check exact segmentation reason on the isolated E-Port',
        command: 'Brocade: fabricshow ; switchshow\nCisco: show interface fc<isl_port> ; show logging logfile | include "segment"',
        expectedOutput: 'All switches in fabric listed with unique Domain IDs; ISL state "Online".',
        abnormalIndication: '"Segmented (Domain ID conflict)" or "Segmented (Zoning database mismatch)".',
        actionIfAbnormal: 'Note the exact conflicting Domain ID or conflicting zone configuration.',
      },
      {
        stepNumber: 2,
        instruction: 'Resolve Domain ID Conflict (If duplicate domain ID detected)',
        command: 'Brocade:\nswitchdisable\nconfigure (enter unique Domain ID)\nswitchenable\n\nCisco:\nconfigure terminal\nfcdomain domain <unique_id> static vsan <vsan_id>\nfcdomain restart vsan <vsan_id>',
        expectedOutput: 'Switch boots with new unique Domain ID without colliding with principal switch.',
        abnormalIndication: 'Switch fails to rejoin fabric.',
        actionIfAbnormal: 'Inspect fabric principal switch priority via `fabricprincipal` or `fcdomain priority`.',
      },
    ],
    emergencyRemediation: [
      'Never force-enable an ISL if zoning databases have unresolvable conflicts; export active zoning configs from both sides first.',
      'Set fabric merge policy consistently across switches.',
      'Assign explicit static Domain IDs per switch during commissioning to prevent dynamic collisions.',
    ],
    verificationChecklist: [
      '`fabricshow` on Brocade displays all switches in a single unified fabric',
      '`show topology vsan <vsan_id>` on Cisco confirms all switches merged',
      'All host paths restore across both redundant fabrics',
    ],
  },
];
